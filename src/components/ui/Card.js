import React from 'react';
import classes from './Card.module.css'

const Card = () => {
  return (
    <div className={classes.cardItem}>
    <div> </div>
    <h1> {name} </h1>
    <p></p>
  </div>
);
  
};

export default Card;
